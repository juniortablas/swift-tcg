/**
 * Shared Shopify types
 *
 * Future home for TypeScript types shared across the storefront integration:
 * - Product, Variant, Collection, Cart, Money, Image, etc.
 * - Normalized app-facing shapes (if we map away from raw Storefront API types)
 * - Request / response helpers used by queries and mutations
 *
 * Do not add real Shopify types yet.
 */

export {};
