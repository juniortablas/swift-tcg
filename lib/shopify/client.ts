/**
 * Storefront API client
 *
 * Future home for the Shopify Storefront API client setup:
 * - Client initialization (endpoint, storefront access token)
 * - Shared fetch / GraphQL request helper
 * - Error handling and response normalization
 *
 * Do not add real Shopify SDK or API calls until the store is connected.
 */

export {};
