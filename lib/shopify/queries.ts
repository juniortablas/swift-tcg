/**
 * GraphQL queries
 *
 * Future home for Storefront API read operations, such as:
 * - Products and product details
 * - Collections
 * - Cart retrieval
 * - Shop / localization metadata
 *
 * Keep query documents here; keep execution in the client layer.
 *
 * Do not add real Shopify GraphQL yet.
 */

export {};
